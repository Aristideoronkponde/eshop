import 'package:flutter/material.dart';

class Repaired extends StatelessWidget {
  const Repaired({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Aristide",
      textDirection: TextDirection.ltr,
    );
  }
}
