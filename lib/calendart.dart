import 'package:flutter/material.dart';

class Calendart extends StatelessWidget {
  const Calendart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Aristide",
      textDirection: TextDirection.ltr,
    );
  }
}
