import 'package:flutter/material.dart';

class InService extends StatelessWidget {
  const InService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Aristide",
      textDirection: TextDirection.ltr,
    );
  }
}
